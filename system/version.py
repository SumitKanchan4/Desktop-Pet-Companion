"""
system/version.py — Single source of truth for the application version.

Bump this before tagging a release.  The updater and About dialog both
read from here so there is never a version mismatch.
"""

APP_VERSION = "0.3.1"

# GitHub repo used for release checks — must match the actual remote.
GITHUB_REPO = "SumitKanchan4/Desktop-Pet-Companion"
